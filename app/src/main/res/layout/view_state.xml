<?xml version="1.0" encoding="utf-8"?>
<!-- Overlay loading / empty / error dùng chung mọi màn hình -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/stateRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:visibility="gone">

    <!-- LOADING -->
    <LinearLayout
        android:id="@+id/stateLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:visibility="gone">

        <ProgressBar
            android:id="@+id/stateSpinner"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:indeterminateTint="@color/kl_brand" />

        <TextView
            android:id="@+id/stateLoadingText"
            style="@style/KL.Text.Body"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/state_loading" />
    </LinearLayout>

    <!-- EMPTY / ERROR (chung khung, khác icon + chữ) -->
    <LinearLayout
        android:id="@+id/stateEmpty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingLeft="@dimen/sp_screen_margin"
        android:paddingRight="@dimen/sp_screen_margin"
        android:visibility="gone">

        <ImageView
            android:id="@+id/stateImage"
            android:layout_width="@dimen/empty_img_w"
            android:layout_height="@dimen/empty_img_h"
            android:scaleType="fitCenter"
            android:alpha="0.9"
            android:contentDescription="@string/cd_empty" />

        <TextView
            android:id="@+id/stateTitle"
            style="@style/KL.Text.Section"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:gravity="center" />

        <TextView
            android:id="@+id/stateBody"
            style="@style/KL.Text.Body"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:gravity="center"
            android:maxWidth="420dp" />

        <TextView
            android:id="@+id/stateRetry"
            style="@style/KL.Button.Primary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:text="@string/action_retry"
            android:visibility="gone" />
    </LinearLayout>
</FrameLayout>
